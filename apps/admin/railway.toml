[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pnpm install && pnpm run build && pnpm run start"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10